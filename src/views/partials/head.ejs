<!DOCTYPE html>
<html lang="en">
    <head>
        <meta http-equiv="x-ua-compatible" content="ie=edge">
	    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/css/bootstrap.css">
        <link rel="stylesheet" href="/css/style.css">
        <title><%= (typeof title === 'undefined') ? '' : title + " - " %><%= SITENAME %></title>
    </head>

    <body class="bg-success">
    	<% if (header) { %>
    	<nav class="container navbar navbar-expand-md bg-dark navbar-dark mb-md-1">
            <a href="/" class="navbar-brand d-none d-md-block"> <%= SITENAME %></a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <i class="fas fa-bars"></i><span class="navbar-brand p-2"> <%= SITENAME %></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <% if(user){%>
                <ul class="navbar-nav">
                    <li class="nav-item pr-2 pl-2">
                        <a href="/" class="nav-link"><i class="fa fa-home fa-2x navicon" aria-hidden="true"></i>  Home</a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item pr-2 pl-2">
                        <a href="/paste/new" class="nav-link"><i class="fa fa-plus fa-2x navicon" aria-hidden="true"></i> New Paste</a>
                    </li>
                </ul>
                <% }%>
                <ul class="navbar-nav ml-auto">
                    <% if(user){%> 
                     <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown">
                          <i class="fa fa-user navicon ml-2"></i> <%= user.username %>
                        </a>
                        <div class="dropdown-menu bg-dark">
                          <a class="dropdown-item" href="#"><i class="fa fa-cog navicon"></i> Config</a>
                          <div class="dropdown-divider"></div>
                          <a class="dropdown-item" href="/logout"><i class="fa fa-sign-in-alt navicon"></i> Logout</a>
                        </div>
                      </li>
                    <%}else{%>
                    <li class="nav-item pr-2 pl-2">                        
                        <a href="/login" class="nav-link">
                        <i class="fa fa-sign-in-alt fa-2x navicon" aria-hidden="true"></i> Singin</a>
                    </li>
                    <%}%>
                </ul>
            </div>
            
        </nav>
        <% } %>